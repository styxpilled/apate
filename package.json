{
  "name": "apate",
  "description": "Generate the greatest Linkedin bio in existence",
  "authors": "styxpiled",
  "version": "0.0.2",
  "scripts": {
    "test": "tsc && node dist/generator.test.js",
    "dev": "tsc -w && node dist/generator.test.js"
  },
  "main": "dist/index.js",
  "devDependencies": {
    "typescript": "^4.6.2"
  }
}
