import ai from './python.ai';
import lib from './python.lib';

/**
 * Python wordlist.
 * @property ai - AI and ML frameworks and libraries.
 * @property lib - Libraries.
 */
const python = {
  /** Python AI and ML frameworks and libraries. */
  ai,
  /** Python libraries. */
  lib,
};

export default python;